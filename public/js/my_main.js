function readURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blaha').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logo").on("change", function () {
    readURL(this);
    
});
///////////////////////////////////////////////////////////
function readURL1(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas1').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos1").on("change", function () {
    readURL1(this);
    
});
///////////////////////////////////////////////////////////
function readURL2(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas2').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos2").on("change", function () {
    readURL2(this);
    
});
///////////////////////////////////////////////////////////
function readURL3(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas3').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos3").on("change", function () {
    readURL3(this);
    
});
///////////////////////////////////////////////////////////
function readURL4(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas4').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}
$("#file-logos4").on("change", function () {
    readURL4(this);
    
});
////////////////////////////////////////////////////////////
function readURL5(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas5').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos5").on("change", function () {
    readURL5(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL6(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas6').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos6").on("change", function () {
    readURL6(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL7(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas7').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos7").on("change", function () {
    readURL7(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL8(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas8').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos8").on("change", function () {
    readURL8(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL9(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas9').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos9").on("change", function () {
    readURL9(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL10(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas10').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos10").on("change", function () {
    readURL10(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL11(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas11').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos11").on("change", function () {
    readURL11(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL12(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas12').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos12").on("change", function () {
    readURL12(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL13(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas13').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos13").on("change", function () {
    readURL13(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL14(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas14').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos14").on("change", function () {
    readURL14(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL15(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas15').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos15").on("change", function () {
    readURL15(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL16(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas16').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos16").on("change", function () {
    readURL16(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL17(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas17').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos17").on("change", function () {
    readURL17(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL18(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas18').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos18").on("change", function () {
    readURL18(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL19(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas19').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos19").on("change", function () {
    readURL19(this);
    
});

/////////////////////////////////////////////////////////////////
function readURL20(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas20').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos20").on("change", function () {
    readURL20(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL21(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas21').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos21").on("change", function () {
    readURL21(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL22(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas22').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos22").on("change", function () {
    readURL22(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL23(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas23').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos23").on("change", function () {
    readURL23(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL24(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas24').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos24").on("change", function () {
    readURL24(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL25(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas25').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos25").on("change", function () {
    readURL25(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL26(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas26').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos26").on("change", function () {
    readURL26(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL27(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas27').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos27").on("change", function () {
    readURL27(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL28(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas28').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos28").on("change", function () {
    readURL28(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL29(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas29').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos29").on("change", function () {
    readURL29(this);
    
});
/////////////////////////////////////////////////////////////////
function readURL30(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blahas30').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-logos30").on("change", function () {
    readURL30(this);
    
});
/////////////////////////////////////////////////////////////////

function blahaURL(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('#blah').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#file-upload").on("change", function () {

    blahaURL(this);

});
//////////////////////////////////////////
function photo1Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo2').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo22").on("change", function () {

    photo1Url(this);

});

/////////////////////////////////////////////////////////////////////

function photo3Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo3 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo33").on("change", function () {

    photo3Url(this);

});

////////////////////////////////////////////////////////////////////

function photo4Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo4 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo44").on("change", function () {

    photo4Url(this);


});

//////////////////////////////////////////////////////////////////



function photo5Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo5 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo55").on("change", function () {

    photo5Url(this);



});

///////////////////////////////////////////////////////////////////////

function photo6Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo6 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo66").on("change", function () {

    photo6Url(this);



});

////////////////////////////////////////////////////////////////////

function photo7Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo7 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo77").on("change", function () {



    photo7Url(this);



});


/////////////////////////////////////////////////////////////////////


function photo8Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo8 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo88").on("change", function () {

    photo8Url(this);



});


/////////////////////////////////////////////////////////////////////

function photo9Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo9 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo99").on("change", function () {



    photo9Url(this);



});

//////////////////////////////////////////////////////////////////////

function photo10Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo10 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo100").on("change", function () {
    photo10Url(this);
});

/////////////////////////////////////////////////////////////////////


function photo11Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo11 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo110").on("change", function () {



    photo11Url(this);



});
/////////////////////////////////////////////////////////////////////////////////


function photo12Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo12 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo120").on("change", function () {
    photo12Url(this);



});

////////////////////////////////////////////////////////////////////////


function photo13Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo13 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo130").on("change", function () {


    photo13Url(this);



});
///////////////////////////////////////////////////////////////////////////////////


function photo14Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo14 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo140").on("change", function () {



    photo14Url(this);



});

/////////////////////////////////////////////////////////////////


function photo15Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo15 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo150").on("change", function () {


    photo15Url(this);



});

///////////////////////////////////////////////////////////////////////

function photo16Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo16 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo160").on("change", function () {



    photo16Url(this);



});
///////////////////////////////////////////////////////////////////////

function photo17Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo17 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo170").on("change", function () {

    photo17Url(this);



});

///////////////////////////////////////////////////////////////////////

function photo18Url(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
            $('.photo18 ').attr('src', e.target.result);
        };
        reader.readAsDataURL(input.files[0]);
    }
}

$("#photo180").on("change", function () {
    photo18Url(this);

});



